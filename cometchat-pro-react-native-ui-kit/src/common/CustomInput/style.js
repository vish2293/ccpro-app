import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    borderWidth: 1,
    borderColor: 'rgba(0,0,0,0.2)',
    paddingLeft: 10,
    borderRadius: 5,
  },
});
